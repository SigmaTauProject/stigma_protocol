

export default class Ship {
	constructor(network) {
		this._network	= network	;
		this.components	= []	;
	}
	////getComponent(id) {
	////	if (!this._components[id])
	////		this._components[id] = createComponent(id);
	////	return this._components[id];
	////	
	////	function createComponent(id) {
	////		if (this.componentTypes[id] == "x") {
	////			return new X(this._network);
	////		}
	////	}
	////}
}



